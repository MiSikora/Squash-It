<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.mehow.squashit">

  <application>
    <activity
        android:name="io.mehow.squashit.report.ReportActivity"
        android:exported="false"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.SquashIt"
        android:windowSoftInputMode="adjustPan" />

    <activity
        android:name="io.mehow.squashit.report.MisconfigurationActivity"
        android:exported="false"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.SquashIt" />

    <activity
        android:name="io.mehow.squashit.screenshot.ScreenshotActivity"
        android:exported="false"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.SquashIt" />

    <provider
        android:name="io.mehow.squashit.report.SquashItInitializer"
        android:authorities="${applicationId}.SquashItInitializer"
        android:exported="false" />
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.squashIt"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>

    <meta-data
        android:name="preloaded_fonts"
        android:resource="@array/preloaded_fonts" />
  </application>
</manifest>
