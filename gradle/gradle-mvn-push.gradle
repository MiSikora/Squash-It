apply plugin: libs.dokka.gradlePluginId
apply plugin: libs.mavenPublish.gradlePluginId

signing {
  def signingKey = findProperty("mavenCentralSigningKey") ?: ""
  if (!signingKey.isEmpty()) {
    useInMemoryPgpKeys signingKey, ""
  }
}
